---
name: scaffold-area-role
description: Use this skill when areas/<area>/roles/<your role> is missing but work is happening in the area.
---

# Scaffold Area Role

Run:

```bash
<skill dir>/scripts/scaffold-area-role.sh <area-name> <role-name>
```

This creates `areas/<area>/roles/<role>/MEMORY.md` by copying `assets/role-memory.md`.